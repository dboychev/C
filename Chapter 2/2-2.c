#include <stdio.h>

main()
{
	// for (int i = 0; i < lim - 1 && (c = getchar()) != '\n' && c != EOF; i++)

	// without || and &&

	for (int i = 0; i < lim; i++)
	{
		if (c == getchar() != '\n')
		{
			if (c != EOF)
			{

			}
		}
	}
}